{
  "name": "bot_monitor",
  "version": "1.0.0",
  "description": "a tg bot that warns users and removes them if they paste ca contracts of solana",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "prosper",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7",
    "mongoose": "^8.9.5",
    "telegraf": "^4.16.3"
  }
}
